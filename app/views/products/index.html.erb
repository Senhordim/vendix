<h2>Produtos</h2>
<hr>

<% @products.each do |product| %>
    <div class="product">
        <h2><%= product.title %></h2>
        <p><%= product.price %></p>
        <%= link_to 'Visualziar', product_path(product.id) %>
    </div>
<% end %>
